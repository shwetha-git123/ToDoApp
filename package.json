{
  "name": "todoproject",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    
    "build": "if-en TYPE=server &&(cd server && npm install)||(cd to-do-app && npm install)",
    "server":"node .server/server.js",
    "to-do-app":"cd to-do-app && npm start"
  },
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
